<%= turbo_stream.replace dom_id(@item) do %>
  <%= render(ItemComponent.new(item: @item, show_controls: true, show_handle: true))%>
<% end %>
<%= turbo_stream.update("toasts", partial: "shared/flash") %>
